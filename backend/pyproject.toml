[project]
name = "ai-studio"
version = "1.0.0"
description = "NxZenAI Studio Backend"
requires-python = ">=3.12"

[tool.black]
line-length = 88

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
asyncio_mode = "auto"

testpaths = [
    "tests"
]